name: UVB E2E 3
on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Select bump type'
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Ensure at least one tag
        run: |
          if ! git describe --tags --abbrev=0 >/dev/null 2>&1; then
            git tag v0.0.0 && git push origin --tags
          fi

      - name: Setup playground2 (idempotent)
        run: |
          mkdir -p playground2
          [ -f playground2/package.json ] || echo '{"name":"demo","version":"0.1.0"}' > playground2/package.json

      - name: Run action from branch
        uses: nyaomaru/universal-version-bump@feature/jsr-version-bump-support
        with:
          release_type: ${{ inputs.release_type }}
          target_path: playground2
          git_tag: 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Show results
        run: |
          echo "::group::git status"
          git status --porcelain
          echo "::endgroup::"
          echo "::group::manifests"
          cat playground2/package.json || true
          echo "::endgroup::"
